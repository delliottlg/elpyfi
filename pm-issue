#!/usr/bin/env bash
# PM Claude Issue Tracker CLI

# Get the directory of this script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Run the issue tracker
"$DIR/venv/bin/python" -m src.issue_tracker "$@"